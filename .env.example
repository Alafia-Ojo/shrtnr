# Server
PORT=3000
DATABASE_PATH=urlshort.db
PUBLIC_URL=http://localhost:3000

# Admin dashboard — set a token to access /dashboard?admin_token=<value>
# If unset, a random token is generated and logged at each startup
ADMIN_TOKEN=
